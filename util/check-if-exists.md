## jQuery object check
```js
if( $('[selector]').length )
{
  // it exists
}

// example
// use this if you are using id to check
if( $('#foo').length )
{
  // it exists
}
```

## Variable check
#### Undefined
```js
if( typeof x === 'undefined' )
{
  // variable 'x' is undefined
}
```
